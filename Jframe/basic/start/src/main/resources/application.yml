server:
  port: 8080
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/jframe
    username: root
    password: root
management:
  endpoints:
    web:
      exposure:
        include=*:




mybatis-plus:
  mapper-locations: classpath:/mapper/*.xml