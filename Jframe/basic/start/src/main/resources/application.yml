server:
  port: 8080
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.195.150:3306/jframe
    username: root
    password: home
management:
  endpoints:
    web:
      exposure:
        include=*:




mybatis-plus:
  mapper-locations: classpath:/mapper/*.xml

jframe:
  security:
    jwt:
      header: Authorization
      # 过期时间，s为单位 7d
      expire: 60 * 60 * 24 * 7
      secret: jframe_jwt_jvciziyaebnr
